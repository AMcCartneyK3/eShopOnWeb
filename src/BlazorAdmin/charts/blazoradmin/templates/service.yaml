apiVersion: v1
kind: Service
metadata:
  name: {{ template "blazoradmin.fullname" . }}
  labels:
    app: {{ template "blazoradmin.name" . }}
    chart: {{ template "blazoradmin.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "blazoradmin.name" . }}
    release: {{ .Release.Name }}
